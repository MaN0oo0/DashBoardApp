@model  IEnumerable<DepartmentVM>
@{
    ViewData["Title"] = "Index";
}


<h2>Welcom To DepartMent</h2>
<div class="row">
    
<a asp-action="Create" asp-controller="Department" class="btn btn-primary text-uppercase m" >Create New Department</a>
</div>
@*<select>
@foreach (var item in ViewBag.data)
{
    <option>@item</option>
}
</select>*@

@*<input type="text" list="mylist" />
<datalist id="mylist">
@foreach (var item in ViewBag.data)
{
    <option>@item.Name</option>
}
</datalist>*@

@*<ul>
    @foreach (var item in ViewBag.data)
{
    <li>@item</li>
}
</ul>*@


  <table class="table tabel-hover table-dark table-striped">
    <thead class="thead-dark">
      <tr >
        <th >ID</th>
        <th>Name</th>
        <th>Code</th>
        <th>Details</th>
        <th>Update</th>
        <th>Delete</th>

      </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
      <tr>
        <td>@item.Id</td>
        <td>@item.Name</td>
        <td>@item.Code</td>
          <td>

            <a asp-action="Details" asp-controller="Department" asp-route-id="@item.Id" class="btn btn-success">Details</a>
           
        </td>
        <td>
             <a asp-action="Update" asp-controller="Department" asp-route-id="@item.Id" class="btn btn-warning">Update</a>
        </td>
        <td>
                        <a asp-action="Delete" asp-controller="Department" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>

        </td>
        
      </tr>
      
        
  
        }
    </tbody>
  </table>

@section Styles{
  
      <link href="~/css/Custom.css" rel="stylesheet"/>
}